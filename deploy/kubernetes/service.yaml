apiVersion: v1
kind: Service
metadata:
  name: openai-compatible-api
  labels:
    app: openai-compatible-api
spec:
  selector:
    app: openai-compatible-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
